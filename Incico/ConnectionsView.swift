import SwiftUI

struct ConnectionsView: View {
    var body: some View {
        NavigationView {
            Text("Connections Content")
                .navigationTitle(String(localized: "Connections"))
        }
    }
}