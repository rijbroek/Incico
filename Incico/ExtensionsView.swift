import SwiftUI

struct ExtensionsView: View {
    var body: some View {
        NavigationView {
            Text("Extensions Content")
                .navigationTitle(String(localized: "Extensions"))
        }
    }
}