import SwiftUI

struct ProfilesView: View {
    var body: some View {
        NavigationView {
            Text("Profiles Content")
                .navigationTitle(String(localized: "Profiles"))
        }
    }
}