import SwiftUI

struct CirclesView: View {
    var body: some View {
        NavigationView {
            Text("Circles Content")
                .navigationTitle(String(localized: "Circles"))
        }
    }
}